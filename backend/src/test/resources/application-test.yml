spring:
  datasource:
    url: jdbc:h2:mem:micro-finance;DB_CLOSE_DELAY=-1
  h2:
    console:
      enabled: true
  jpa:
    show-sql: false
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop